name: VierGewinnt
type: gradio-space
sdk: static

metadata:
  space_id: Chris4K/VierGewinnt
  original_url: https://huggingface.co/spaces/Chris4K/VierGewinnt

deploy:
  targets:
    - public-repo
    - huggingface

build:
  command: bash build.sh

test:
  command: echo "Tests not yet implemented"
